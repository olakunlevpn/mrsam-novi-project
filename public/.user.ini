; PHP upload limits for the admin panel (PHP-FPM reads .user.ini from the docroot).
; post_max_size must be >= upload_max_filesize to leave room for other form fields.
upload_max_filesize = 100M
post_max_size = 110M
memory_limit = 256M
max_file_uploads = 20
